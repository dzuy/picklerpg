import {PlayerCreator} from './player-creator';
import {PATTERNS} from './engine/patterns';
import {PERSONALITIES} from './engine/opponent-brain';
import {PLAYER_PROFILES,ARCHETYPES} from './engine/player-profiles';
import {Match} from './match';
import {ShotLab,LAB_TYPES,type LabCondition} from './shot-lab';
import {SHOT_FAMILIES} from './engine/shot-families';
import {sameShotIntent,parseShotIntent} from './engine/shot-intent';
import type {ShotIntent,ShotType,ShotTarget} from './engine/model';
import {SHOT_INTENT_SCHEMA,targetLabel} from './engine/shot-intent';
import './style.css';
import {Simulation,RALLY} from './simulation';
import {CourtScene} from './scene';
import {preloadAthletes} from './athlete';
const app=document.querySelector<HTMLDivElement>('#app')!;
app.innerHTML=`
<header class="header"><a class="brand" href="/" aria-label="Pickle RPG home"><span class="brand-ball">⠿</span> PICKLE<span>RPG</span></a><button id="open-settings" class="header-icon" aria-label="Settings" title="Settings" aria-haspopup="dialog"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9.2 3-.6 2.2-1.5.9L4.9 6 2.8 9.6l1.6 1.6v1.7l-1.6 1.6 2.1 3.6 2.2-.2 1.5.9.6 2.2h4.2l.6-2.2 1.5-.9 2.2.2 2.1-3.6-1.6-1.6v-1.7l1.6-1.6L17.7 6l-2.2.1-1.5-.9-.6-2.2Z"/><circle cx="11.3" cy="12" r="3"/></svg></button></header>
<main><section class="play-section">
<div class="court-wrap"><div id="court"></div><div class="court-top"><span class="court-chip"><i></i> GARDEN COURT</span><div class="score" aria-label="Match score"><div class="score-row score-row-home"><span>YOU &amp; FINN</span><b id="score-home">0</b></div><div class="score-row score-row-away"><span>JULES &amp; RIO</span><b id="score-away">0</b></div></div></div><div id="court-result" class="court-result" role="status" aria-live="assertive" hidden><span>POINT OVER</span><strong id="court-result-title"></strong><small id="court-result-detail"></small></div><div class="court-bottom"><span>20 × 44 FT <span class="small-dot">·</span> REGULATION COURT</span><span class="team-key"><i></i> YOUR TEAM <i></i> OPPONENTS</span></div></div>
<div class="camera-slider"><span aria-hidden="true">−</span><input id="camera-distance" type="range" min="0" max="100" value="50" step="1" aria-label="Camera zoom" aria-valuetext="Default view"><span aria-hidden="true">+</span><button id="reset-camera" class="camera-reset" aria-label="Reset camera view" title="Reset camera view">↺</button></div>

<div class="rally-strip"><div class="eyebrow">THE PATTERN <span>01 / PRESSURE THE MIDDLE</span></div><ol id="timeline">${['Serve','Deep return','Drive middle','High block','Overhead'].map((s,i)=>`<li data-step="${i}"><span class="step-number">${i+1}</span><span>${s}</span></li>`).join('')}</ol></div>
</section><aside><button id="back-to-play" class="back-to-play">← Back to play</button><div class="aside-heading"><span class="eyebrow">PATTERN 01</span><span class="difficulty">INTERMEDIATE +</span></div><h2>Pressure.<br>Pop-up.<br><em>Put-away.</em></h2><p class="pattern-description">Create a weak ball through the middle. Recognize your chance to finish.</p><div class="aside-divider"></div><div id="decision" aria-live="polite"></div><div class="coach"><span class="coach-icon">✳</span><div><div class="eyebrow">COURT SENSE</div><p id="cue"></p></div></div><div class="aside-foot"><span class="script-dot"></span> Scripted opponents <span>·</span> Automatic positioning</div><section id="match-panel" hidden></section><section id="lab-panel" hidden><div class="eyebrow">SHOT LAB</div><h2>Meet your<br><em>shot options.</em></h2><p class="pattern-description">Compare one shot at a time from a prepared contact. No opponent response or scoring.</p><details open><summary>Your decision menu</summary><label for="decision-situation">Your contact</label><select id="decision-situation"><option value="off">Off — free shot exploration</option><option value="serve">Serve</option><option value="return">Return after bounce</option><option value="drive">Deep bounced ball</option><option value="reset">Low transition ball</option><option value="counter">Incoming attack</option><option value="dink">Kitchen exchange</option><option value="overhead">High put-away</option></select><label for="decision-choice">Available shot</label><select id="decision-choice" disabled></select><p id="decision-reason" role="status"></p></details><details><summary>Opponent decisions</summary><label for="opponent-situation">Opponent contact</label><select id="opponent-situation"><option value="off">Off — your shot</option><option value="pressure">Incoming attack</option><option value="low">Low transition ball</option><option value="high">High put-away</option><option value="return">Serve return</option><option value="deep">Deep bounced ball</option><option value="dink">Kitchen exchange</option></select><label for="opponent-aggression">Opponent style</label><select id="opponent-aggression"><option value="0.7">Aggressive</option><option value="0.3">Patient</option></select><p id="opponent-reason" role="status"></p><p>Prepared opponent contact: play the selected response from the far side. This is a single decision, not a full point.</p></details><label for="family">Shot family</label><select id="family">${LAB_TYPES.map(type=>`<option value="${type}" ${type==='drive'?'selected':''}>${SHOT_FAMILIES[type].name}</option>`).join('')}</select><label for="condition">Contact condition</label><select id="condition"><option value="typical">Typical contact</option><option value="low">Ankle-high contact</option><option value="kitchen">Inside kitchen, before bounce</option></select><label for="target-choice">Target</label><select id="target-choice"><option value="default">Family default</option><option value="middle">Middle seam</option><option value="crosscourt">Crosscourt</option><option value="line">Down the line</option><option value="wide">Wide</option><option value="open-court">Open court</option><option value="body">Body</option><option value="feet">Feet</option><option value="backhand-side">Backhand side</option></select><div id="zone-controls" hidden><label for="target-depth">Target depth</label><select id="target-depth"><option value="deep">Deep</option><option value="transition">Transition</option><option value="kitchen">Kitchen</option></select></div><div id="player-controls" hidden><label for="target-player">Opponent</label><select id="target-player"><option value="opponent-left">Jules</option><option value="opponent-right">Rio</option></select><label for="opponent-hand">Opponent handedness</label><select id="opponent-hand"><option value="right">Right-handed</option><option value="left">Left-handed</option></select></div><label for="opponent-layout">Opponent positions</label><select id="opponent-layout"><option value="balanced">Balanced</option><option value="left">Both shading left</option><option value="right">Both shading right</option></select><details id="flight-controls"><summary>Flight intent</summary><label for="intent-pace">Pace</label><select id="intent-pace"><option value="default">Family default</option><option value="soft">Soft</option><option value="medium">Medium</option><option value="fast">Fast</option></select><label for="intent-shape">Shape</label><select id="intent-shape"><option value="default">Family default</option><option value="flat">Flat</option><option value="arc">Arc</option><option value="descending">Descending</option></select><label for="intent-clearance">Requested net clearance</label><select id="intent-clearance"><option value="0.12">0.12 m</option><option value="0.4">0.40 m</option><option value="0.8">0.80 m</option><option value="1.5">1.50 m</option></select><label for="intent-tactic">Tactical purpose</label><select id="intent-tactic"><option value="sustain">Sustain</option><option value="pressure">Pressure</option><option value="advance">Advance</option><option value="neutralize">Neutralize</option><option value="finish">Finish</option></select><label for="intent-aggression">Aggression</label><select id="intent-aggression"><option value="0">Low</option><option value="0.5" selected>Balanced</option><option value="1">High</option></select></details><details><summary>Execution</summary><label for="difficulty-preset">Contact difficulty</label><select id="difficulty-preset"><option value="comfortable">Comfortable forehand</option><option value="stretched">Stretched forehand</option><option value="backhand">Low backhand</option><option value="backward">Moving backward</option><option value="feet">Transition ball at feet</option></select><p id="rating-comparison"></p><label for="execution-enabled">Execution variance</label><select id="execution-enabled"><option value="off">Off — ideal flight</option><option value="on">On — sampled flight</option></select><label for="execution-skill">Player skill</label><select id="execution-skill"><option value="40">3.0 benchmark · skill 40</option><option value="70" selected>4.0 benchmark · skill 70</option><option value="90">5.0 benchmark · skill 90</option><option value="95">Advanced · skill 95</option></select><label for="execution-balance">Balance</label><select id="execution-balance"><option value="1">Set</option><option value="0.5">Moving</option><option value="0">Stretched</option></select><label for="incoming-speed">Incoming pace</label><select id="incoming-speed"><option value="5">Soft</option><option value="12" selected>Medium</option><option value="25">Fast</option></select><button id="execution-sample">New execution sample</button><p id="execution-stats" role="status"></p><p>Target marker shows your intent. The flight guide shows execution. Replay keeps the same sample.</p></details><p id="family-description"></p><p id="lab-reason" role="status"></p><button id="lab-play" class="shot-button">Play shot ↗</button><p id="lab-stats"></p><p class="guided-note">Targets respond to opponent positions. These single-shot previews do not predict a winner.</p></section></aside></main><footer><span>READ THE COURT. MAKE THE CALL.</span><span><kbd>Space</kbd> play shot / pause <kbd>R</kbd> restart</span></footer><dialog id="game-settings" aria-labelledby="settings-title"><div class="settings-heading"><div><div class="eyebrow">GAME CONTROLS</div><h2 id="settings-title">Settings</h2></div><button id="close-settings" aria-label="Close settings">✕</button></div><p>Set your preferred view and pace for every game.</p><label for="speed">Default playback speed</label><select id="speed"><option value="0.975">0.65× · Slower</option><option value="1.5" selected>1× · Normal</option><option value="2.25">1.5× · Faster</option></select><p class="settings-help">Changes animation speed. Every team contact still waits for your choice.</p><label class="settings-toggle" for="guides"><span>Flight guide<small>Show the ball path and target marker.</small></span><input id="guides" type="checkbox" role="switch" checked></label><p id="settings-status" role="status">Changes apply immediately.</p><nav class="settings-destinations" aria-label="More game options"><button data-panel="settings">Game setup <span aria-hidden="true">→</span></button><button data-panel="insights">Session review <span aria-hidden="true">→</span></button></nav><button id="done-settings" class="shot-button">Done</button></dialog>`;
document.querySelector('#game-settings .settings-heading')!.insertAdjacentHTML('afterend','<div id="settings-primary-controls"><label for="mode">Game type</label><select id="mode" aria-label="Game type"><option value="match" selected>Full game · doubles</option><option value="rally">Guided rally</option><option value="lab">Shot lab</option></select><button id="restart" class="settings-restart">Restart current game <span aria-hidden="true">↻</span></button></div>');
document.querySelector('#settings-primary-controls')!.insertAdjacentHTML('afterend','<label class="settings-toggle partner-autonomy-setting" for="partner-autonomy"><span>Finn auto-play<small>Let Finn choose his own shots when the ball comes to him.</small></span><span class="settings-switch-control"><strong id="partner-autonomy-state">Off</strong><input id="partner-autonomy" type="checkbox" role="switch"></span></label>');
app.insertAdjacentHTML('beforeend','<dialog id="shot-drawer" aria-labelledby="shot-drawer-title"><div class="drawer-heading"><div><div class="eyebrow">SHOT MENU</div><h2 id="shot-drawer-title">More options</h2></div><button id="close-shot-drawer" aria-label="Close more options">✕</button></div><p id="shot-drawer-description">Other playable choices for this contact.</p><div id="more-options-list"></div></dialog>');
await preloadAthletes();
const guided=new Simulation();const match=new Match();let sim:Simulation|Match=match;const lab=new ShotLab();let mode='match';let scene:CourtScene;
try{scene=new CourtScene(document.querySelector('#court')!)}catch(error){document.querySelector('#court')!.innerHTML='<div class="webgl-error"><h2>3D rendering is unavailable</h2><p>Enable hardware acceleration in your browser, then reload to play.</p></div>';throw error}
const byId=(id:string)=>document.getElementById(id)!;
let speed=1.5,guides=true,cameraDistance=50,lastUI='';
const creator=new PlayerCreator(player=>{
 match.practice=null;match.setPlayerDesign(player);scene.setPlayerDesign(player);
 document.querySelector('.score-row-home > span')!.textContent=`${player.name} & FINN`;
 (byId('mode') as HTMLSelectElement).value='match';byId('mode').dispatchEvent(new Event('change'));
});
const savedPlayer=creator.activePlayer;
if(savedPlayer){match.setPlayerDesign(savedPlayer);scene.setPlayerDesign(savedPlayer);document.querySelector('.score-row-home > span')!.textContent=`${savedPlayer.name} & FINN`}
byId('open-settings').insertAdjacentHTML('beforebegin','<button class="player-design-entry" id="open-player-design" aria-haspopup="dialog">Player Design</button>');
byId('open-player-design').addEventListener('click',()=>creator.open());
creator.dialog.addEventListener('close',()=>byId('open-player-design').focus());
const escapeText=(text:string)=>text.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));

let activePanel='play';
function showPanel(panel:string){activePanel=panel;document.body.dataset.panel=panel;document.querySelectorAll<HTMLButtonElement>('[data-panel]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.panel===panel)))}
document.querySelectorAll<HTMLButtonElement>('[data-panel]').forEach(b=>b.addEventListener('click',()=>{showPanel(b.dataset.panel!);(byId('game-settings') as HTMLDialogElement).close();byId('back-to-play').focus()}));
byId('back-to-play').addEventListener('click',()=>{showPanel('play');byId('open-settings').focus()});
document.body.classList.add('match-mode');byId('match-panel').hidden=false;byId('restart').title='Restart the game (R)';byId('restart').setAttribute('aria-label','Restart');showPanel('play');


function submit(){if(mode==='lab'){if(!lab.issue)lab.play();lastUI='';updateUI();return}if(sim.state.phase==='decision'&&(mode!=='match'||sim.state.possession==='home')){sim.submitIntent(sim.availableIntents[0]);updateUI()}}
function reset(){mode==='lab'?lab.reset():sim.reset();lastUI='';updateUI()}
function pause(){const state=mode==='lab'?lab.state:sim.state;if(state.phase==='flight'){state.paused=!state.paused;lastUI='';updateUI()}}
byId('restart').addEventListener('click',()=>{reset();closeSettings()});
const settingsDialog=byId('game-settings') as HTMLDialogElement;
const shotDrawer=byId('shot-drawer') as HTMLDialogElement;
let shotDrawerCloseTimer:number|undefined,settingsCloseTimer:number|undefined;
function openShotDrawer(){if(shotDrawerCloseTimer)window.clearTimeout(shotDrawerCloseTimer);shotDrawer.classList.remove('is-closing');if(!shotDrawer.open)shotDrawer.showModal()}
function closeShotDrawer(){if(!shotDrawer.open||shotDrawer.classList.contains('is-closing'))return;shotDrawer.classList.add('is-closing');const delay=matchMedia('(prefers-reduced-motion: reduce)').matches?0:180;shotDrawerCloseTimer=window.setTimeout(()=>{shotDrawer.close();shotDrawer.classList.remove('is-closing');shotDrawerCloseTimer=undefined},delay)}
function openSettings(){if(settingsCloseTimer)window.clearTimeout(settingsCloseTimer);settingsDialog.classList.remove('is-closing');if(!settingsDialog.open)settingsDialog.showModal()}
function closeSettings(){if(!settingsDialog.open||settingsDialog.classList.contains('is-closing'))return;settingsDialog.classList.add('is-closing');const delay=matchMedia('(prefers-reduced-motion: reduce)').matches?0:180;settingsCloseTimer=window.setTimeout(()=>{settingsDialog.close();settingsDialog.classList.remove('is-closing');settingsCloseTimer=undefined},delay)}
byId('open-settings').addEventListener('click',openSettings);
for(const id of ['close-settings','done-settings'])byId(id).addEventListener('click',closeSettings);
settingsDialog.addEventListener('click',event=>{if(event.target===settingsDialog)closeSettings()});
settingsDialog.addEventListener('cancel',event=>{event.preventDefault();closeSettings()});
byId('close-shot-drawer').addEventListener('click',closeShotDrawer);
shotDrawer.addEventListener('click',event=>{if(event.target===shotDrawer)closeShotDrawer()});
shotDrawer.addEventListener('cancel',event=>{event.preventDefault();closeShotDrawer()});
try{const saved=JSON.parse(localStorage.getItem('pickle-rpg-controls-v1')??'null');if(saved&&typeof saved==='object'){if(saved.playbackBase===1.5&&[.975,1.5,2.25].includes(saved.speed))speed=saved.speed;else if([.65,1,1.5].includes(saved.speed))speed=saved.speed===.65?.975:1.5;if(typeof saved.guides==='boolean')guides=saved.guides;if(typeof saved.partnerAutonomy==='boolean')match.partnerAutonomy=saved.partnerAutonomy;if(typeof saved.cameraDistance==='number'&&Number.isFinite(saved.cameraDistance)&&saved.cameraDistance>=0&&saved.cameraDistance<=100)cameraDistance=saved.cameraDistance}}catch{/* Defaults remain usable when storage is unavailable or invalid. */}
(byId('speed') as HTMLSelectElement).value=String(speed);(byId('guides') as HTMLInputElement).checked=guides;(byId('partner-autonomy') as HTMLInputElement).checked=match.partnerAutonomy;byId('partner-autonomy-state').textContent=match.partnerAutonomy?'On':'Off';scene.setGuides(guides);
function saveControls(){try{localStorage.setItem('pickle-rpg-controls-v1',JSON.stringify({speed,guides,cameraDistance,partnerAutonomy:match.partnerAutonomy,playbackBase:1.5}));byId('settings-status').textContent='Saved on this device. Changes apply immediately.'}catch{byId('settings-status').textContent='Applied for this visit. Your browser could not save these settings.'}}
byId('speed').addEventListener('change',e=>{speed=Number((e.target as HTMLSelectElement).value);saveControls()});
function applyCamera(){const slider=byId('camera-distance') as HTMLInputElement;slider.value=String(cameraDistance);scene.setCamera(100-cameraDistance);slider.setAttribute('aria-valuetext',cameraDistance===50?'Default view':cameraDistance>50?'Closer view':'Farther view')}
applyCamera();
const cameraSettings=document.createElement('section');cameraSettings.className='settings-camera';
cameraSettings.innerHTML='<label id="camera-settings-label">Court camera</label><div class="camera-presets"><button data-camera="0">Courtside</button><button data-camera="50">Tactical</button><button data-camera="100">Overhead</button></div><p>Drag the court to orbit. Automatic tracking yields to your view.</p>';
settingsDialog.insertBefore(cameraSettings,byId('settings-status'));
cameraSettings.append(document.querySelector('.camera-slider')!);
byId('camera-distance').setAttribute('aria-label','Camera distance and height');
for(const button of Array.from(cameraSettings.querySelectorAll<HTMLButtonElement>('[data-camera]')))button.addEventListener('click',()=>{const distance=Number(button.dataset.camera);cameraDistance=100-distance;scene.resetCamera(distance);(byId('camera-distance') as HTMLInputElement).value=String(cameraDistance);byId('camera-distance').setAttribute('aria-valuetext',button.textContent!);saveControls()});
byId('camera-distance').addEventListener('input',e=>{cameraDistance=Number((e.target as HTMLInputElement).value);applyCamera();saveControls()});
byId('reset-camera').addEventListener('click',()=>{cameraDistance=50;scene.resetCamera(50);const slider=byId('camera-distance') as HTMLInputElement;slider.value='50';slider.setAttribute('aria-valuetext','Default view');saveControls()});
byId('guides').addEventListener('change',e=>{guides=(e.target as HTMLInputElement).checked;scene.setGuides(guides);saveControls()});
byId('partner-autonomy').addEventListener('change',e=>{match.partnerAutonomy=(e.target as HTMLInputElement).checked;byId('partner-autonomy-state').textContent=match.partnerAutonomy?'On':'Off';lastUI='';saveControls();updateUI()});
document.addEventListener('keydown',event=>{if(settingsDialog.open||creator.dialog.open)return;if(event.target instanceof HTMLElement&&event.target.closest('button, input, select, textarea, a'))return;if(event.code==='Space'){event.preventDefault();mode==='lab'?(lab.state.phase==='flight'?pause():submit()):sim.state.phase==='decision'?submit():sim.state.phase==='complete'?(mode==='match'&&!match.scoring.winner?(match.nextPoint(),lastUI='',updateUI()):reset()):pause()}if(event.key.toLowerCase()==='r')reset()});
byId('mode').addEventListener('change',e=>{mode=(e.target as HTMLSelectElement).value;showPanel('play');sim=mode==='match'?match:guided;document.body.classList.toggle('match-mode',mode==='match');byId('match-panel').hidden=mode!=='match';byId('restart').title=mode==='match'?'Restart the game (R)':'Restart the rally (R)';document.body.classList.toggle('lab-mode',mode==='lab');byId('lab-panel').hidden=mode!=='lab';reset()});
function selectLab(){
 lab.difficultyPreset=(byId('difficulty-preset') as HTMLSelectElement).value;
 lab.decisionSituation=(byId('decision-situation') as HTMLSelectElement).value;
 lab.opponentSituation=(byId('opponent-situation') as HTMLSelectElement).value;lab.opponentAggression=Number((byId('opponent-aggression') as HTMLSelectElement).value);
 lab.variance=(byId('execution-enabled') as HTMLSelectElement).value==='on';lab.skill=Number((byId('execution-skill') as HTMLSelectElement).value);lab.balance=Number((byId('execution-balance') as HTMLSelectElement).value);lab.incomingSpeed=Number((byId('incoming-speed') as HTMLSelectElement).value);

 lab.pace=(byId('intent-pace') as HTMLSelectElement).value as typeof lab.pace;lab.shape=(byId('intent-shape') as HTMLSelectElement).value as typeof lab.shape;lab.clearance=Number((byId('intent-clearance') as HTMLSelectElement).value);lab.tactic=(byId('intent-tactic') as HTMLSelectElement).value as typeof lab.tactic;lab.aggression=Number((byId('intent-aggression') as HTMLSelectElement).value);

 const choice=(byId('target-choice') as HTMLSelectElement).value;
 const playerTarget=['body','feet','backhand-side'].includes(choice);
 byId('player-controls').hidden=!playerTarget;byId('zone-controls').hidden=playerTarget||choice==='default';
 lab.target=choice==='default'?null:playerTarget?{kind:'player',playerId:(byId('target-player') as HTMLSelectElement).value,aim:choice} as ShotTarget:{kind:'zone',zone:choice,depth:(byId('target-depth') as HTMLSelectElement).value} as ShotTarget;
 lab.opponentLayout=(byId('opponent-layout') as HTMLSelectElement).value as typeof lab.opponentLayout;lab.opponentHand=(byId('opponent-hand') as HTMLSelectElement).value as typeof lab.opponentHand;
 lab.select((byId('family') as HTMLSelectElement).value as ShotType,(byId('condition') as HTMLSelectElement).value as LabCondition);lastUI='';updateUI()}
for(const id of ['difficulty-preset','decision-situation','opponent-situation','opponent-aggression','execution-enabled','execution-skill','execution-balance','incoming-speed','target-choice','target-depth','target-player','opponent-hand','opponent-layout','intent-pace','intent-shape','intent-clearance','intent-tactic','intent-aggression'])byId(id).addEventListener('change',selectLab);
byId('decision-choice').addEventListener('change',()=>{const option=lab.decisionOptions.find(o=>o.intent.type===(byId('decision-choice') as HTMLSelectElement).value);if(option){lab.chooseIntent(option.intent);lastUI='';updateUI()}});
byId('execution-sample').addEventListener('click',()=>{lab.seed=(lab.seed+1)>>>0;selectLab()});
byId('family').addEventListener('change',selectLab);byId('condition').addEventListener('change',selectLab);byId('lab-play').addEventListener('click',submit);
function updateUI(){
 if(mode!=='match'){byId('court-result').hidden=true;document.querySelector('.court-wrap')?.classList.remove('has-result')}
 if(mode==='match'){updateMatchUI();return}
 if(mode==='lab'){
  const state=lab.state,key=`lab:${lab.type}:${lab.condition}:${state.phase}:${state.paused}`;if(key===lastUI)return;lastUI=key;
  const choice=byId('decision-choice') as HTMLSelectElement;choice.innerHTML=lab.decisionOptions.map(o=>`<option value="${o.intent.type}" ${o.intent.type===lab.shot.intent.type?'selected':''}>${o.label}</option>`).join('');choice.disabled=!lab.decisionOptions.length||state.phase!=='decision';
  byId('decision-reason').textContent=lab.decisionOptions.find(o=>o.intent.type===lab.shot.intent.type)?.reason??'Choose a prepared contact to see contextual options.';
  (byId('opponent-situation') as HTMLSelectElement).disabled=lab.decisionSituation!=='off';
  byId('opponent-reason').textContent=lab.opponentDecision?`${SHOT_FAMILIES[lab.opponentDecision.intent.type].name}: ${lab.opponentDecision.reason}`:'';
  for(const id of ['family','condition','target-choice','target-depth','target-player','opponent-hand','opponent-layout','intent-pace','intent-shape','intent-clearance','intent-tactic','intent-aggression'])(byId(id) as HTMLSelectElement).disabled=lab.opponentSituation!=='off'||lab.decisionSituation!=='off';
  byId('family-description').textContent=lab.shot.description+' Target: '+targetLabel(lab.shot.intent.target)+'.';byId('lab-reason').textContent=lab.issue??(state.phase==='complete'?(lab.execution?.outcome==='net'?'Shot stopped at the net. Replay or try a new sample.':lab.shot.legs[0].bounceAtEnd?'Shot landed. Replay it or try another family.':'Target reached. No opponent response simulated.'):'Contact is suitable for this family.');
  (byId('lab-play') as HTMLButtonElement).disabled=!!lab.issue||state.phase==='flight';byId('lab-play').textContent=state.phase==='complete'?'Replay shot ↺':'Play shot ↗';
  byId('rating-comparison').textContent=lab.comparison.map(c=>`Skill ${c.skill}: quality ${(c.quality*100).toFixed(0)}%, spread ±${c.dispersion.toFixed(2)} m`).join(' · ');
  byId('execution-stats').textContent=lab.execution?`Sample ${lab.seed} · Execution quality ${(lab.execution.quality*100).toFixed(0)}% · Endpoint deviation ${lab.execution.endpointError.toFixed(2)} m · ${lab.execution.outcome==='net'?'Net contact':lab.execution.outcome==='out'?'Out':lab.execution.outcome==='in'?'In court':'Target-plane preview'}`:'Ideal execution';
  byId('lab-stats').textContent=`Contact height: ${lab.shot.contact.y.toFixed(2)} m · ${lab.issue?'Unavailable':`Flight: ${lab.shot.legs[0].duration.toFixed(2)} s · Intended peak: ${lab.generated!.apex.toFixed(2)} m · Intended net clearance: ${lab.generated!.netClearance.toFixed(2)} m`}`;
  return;
 }

 const s=sim.state;const key=`${s.phase}:${s.shotIndex}:${s.paused}`;if(key===lastUI)return;lastUI=key;
 const shot=sim.shot,decision=s.phase==='decision',done=s.phase==='complete';
 byId('score-home').textContent=String(s.score.home);byId('score-away').textContent=String(s.score.away);
 
 document.querySelectorAll<HTMLElement>('[data-step]').forEach(el=>{const i=Number(el.dataset.step);el.className=done||i<s.shotIndex?'finished':i===s.shotIndex?'current':'';el.querySelector('.step-number')!.textContent=done||i<s.shotIndex?'✓':String(i+1);if(i===s.shotIndex)el.setAttribute('aria-current','step');else el.removeAttribute('aria-current')});
 byId('cue').textContent=done?'The drive earned an attackable fourth ball. The overhead finished it. In live play, a drive can also invite a strong counter—read the response before committing.':shot.cue;
 const badge=done?'PATTERN COMPLETE':decision?'YOUR DECISION':shot.actor==='you'?'YOUR SHOT':'OPPONENT RESPONSE';
 const titles:Record<number,string>={0:'Start with depth.',2:'Find the seam.',4:'Take your chance.'};
 byId('decision').innerHTML=done?`<div class="decision-label won">✓ ${badge}</div><h3>That’s the pattern.</h3><p class="decision-description">Middle pressure → high block → overhead winner.</p><div class="result-stats"><div><strong>5</strong><span>shots</span></div><div><strong>3</strong><span>decisions</span></div><div><strong>+1</strong><span>point</span></div></div><button id="shot-button" class="shot-button replay">Play it again <span>↺</span></button>`:`<div class="decision-label ${decision?'':'playing'}"><span>${decision?'Ⅱ':'↗'}</span> ${badge} <span class="shot-count">${s.shotIndex+1} / 5</span></div><h3>${decision?titles[s.shotIndex]:shot.title}</h3><p class="decision-description">${shot.description}</p>${decision?`<button id="shot-button" class="shot-button"><span><small>${s.shotIndex===0?'SERVE':s.shotIndex===2?'THIRD SHOT':'ATTACK'}</small>${shot.title}</span><span class="arrow">↗</span></button><div class="shot-meta"><span>${shot.intent.pace.toUpperCase()} PACE</span><span>${targetLabel(shot.intent.target).toUpperCase()}</span></div><p class="guided-note">One shot is available in this guided pattern.</p>`:`<div class="watching"><span class="watching-ball"></span><span>${s.paused?'Rally paused':'Watch the court'}<small>${s.paused?'Press Space to resume.':'Play stops before your next contact.'}</small></span></div>`}`;
 byId('shot-button')?.addEventListener('click',done?reset:submit);
}
function choiceCopy(intent:ShotIntent){
 if(intent.type!=='serve')return {name:SHOT_FAMILIES[intent.type].name,detail:targetLabel(intent.target)};
 if(intent.target.kind==='player')return {name:'Body serve',detail:'Jam the receiver'};
 if(intent.intendedNetClearance>1)return {name:'Lob serve',detail:'High and deep'};
 if(intent.target.depth==='transition'&&intent.target.zone==='wide')return {name:'Short outside',detail:'Shallow outside corner'};
 if(intent.target.depth==='transition')return {name:'Short topspin',detail:'Short with extra margin'};
 if(intent.target.zone==='wide'&&intent.pace==='fast')return {name:'Wide power',detail:'Fast toward the sideline'};
 if(intent.target.kind==='zone'&&intent.target.zone==='wide')return {name:'Wide slice',detail:'Pull the receiver wide'};
 if(intent.shape==='arc')return {name:'Topspin deep',detail:'Heavy shape · back third'};
 return {name:'Deep flat',detail:'Fast · back of the box'};
}
function choiceButton(intent:ShotIntent,index:number){const copy=choiceCopy(intent);return `<button class="shot-button match-choice" data-choice="${index}"><span><strong>${copy.name}</strong><span class="choice-target">${copy.detail}</span>${match.shot.actor==='partner'&&match.recommendationType===intent.type?'<small>Finn recommends</small>':''}</span><span aria-hidden="true">↗</span></button>`}
function pointResultCopy(){
 const result=match.state.result,replayAtEnd=match.replayIndex===match.replayFrames.length-1&&!match.replayPlaying;if(match.state.phase!=='complete'||!result||match.replayIndex!==null&&!replayAtEnd)return null;
 const title=result.reason==='net'?'Net!':result.reason==='out'?'Out!':result.reason==='double-bounce'?'Double bounce!':result.reason==='failed-return'?'Missed!':result.reason==='unreturned-attack'?'Winner!':result.reason==='body-hit'?'Body shot!':'Winner!';
 return {title,detail:result.winner==='home'?'Your team wins the rally.':'Opponents win the rally.'};
}
function clock(seconds:number){const whole=Math.max(0,Math.round(seconds));return `${Math.floor(whole/60)}:${String(whole%60).padStart(2,'0')}`}
function analysisCards(){
 const grouped=new Map<number,typeof match.pointRecords>();for(const record of match.pointRecords){const rows=grouped.get(record.shotIndex)??[];rows.push(record);grouped.set(record.shotIndex,rows)}
 const priority=['Risky','Better target available','Good choice','Review target'];
 return [...grouped].map(([shotIndex,rows])=>{const intent=rows[0].intent,actor=intent.actor==='you'?'You':'Finn',shot=SHOT_FAMILIES[intent.type].name,target=targetLabel(intent.target),assessment=priority.map(label=>rows.find(row=>row.label===label)).find(Boolean)??rows[0],label=assessment.label==='Better target available'?'Better target':assessment.label,reason=assessment.label==='Review target'?`${target} was playable, but it did not create a clear tactical advantage from this position.`:assessment.reason;return `<article class="analysis-shot" data-shot-index="${shotIndex}"><header><span>SHOT ${shotIndex+1} · ${actor.toUpperCase()}</span><strong>${shot} · ${target}</strong></header><p><b>${label}</b><span>${reason}</span></p></article>`}).join('')||'<p class="analysis-empty">No player decisions were recorded for this point.</p>';
}
function updateMatchUI(){
 const s=match.state,score=match.scoring,key=`match:${match.point}:${s.phase}:${s.shotIndex}:${s.paused}:${score.call}:${score.winner}:${match.brainStatus}:${match.thinking}:${match.practice}:${match.variation}:${match.customBusy}:${match.customStatus}:${match.replayIndex===null?'live':match.replayPlaying?'replaying':'scrubbing'}`;if(key===lastUI)return;lastUI=key;
 byId('score-home').textContent=String(score.score.home);byId('score-away').textContent=String(score.score.away);
 const choiceEntries=match.availableIntents.map((intent,index)=>({intent,index})).filter(({intent})=>intent.source!=='text');
 const primaryChoices=choiceEntries.slice(0,4),moreChoices=choiceEntries.slice(4);
 byId('shot-drawer-title').textContent=moreChoices[0]?.intent.type==='serve'?'More serves':'More options';
 byId('shot-drawer-description').textContent=moreChoices[0]?.intent.type==='serve'?'Try a less common serve for a different look.':'Other playable choices for this contact.';
 byId('more-options-list').innerHTML=moreChoices.map(({intent,index})=>choiceButton(intent,index)).join('');
 if(!moreChoices.length&&shotDrawer.open)closeShotDrawer();
 const playerDecision=s.phase==='decision'&&s.possession==='home'&&!(match.partnerAutonomy&&s.currentHitter==='partner');
 const finalShotByHome=match.shot.actor==='you'||match.shot.actor==='partner';
 const endTitle=s.phase==='complete'&&s.result?.reason==='net'?(finalShotByHome?'Hit the net! Side out!':'They hit the net! Point won!'):s.phase==='complete'?'Point complete.':null;
 const endDetail=s.phase==='complete'&&s.result?.reason==='net'?(finalShotByHome?'The ball caught the net and dropped on your side.':'The opponent’s ball caught the net and dropped on their side.'):`${s.result?.winner==='home'?'Your team':'Opponents'} won the rally: ${s.result?.reason.replaceAll('-',' ')}.`;
 const courtResult=pointResultCopy(),courtResultBanner=byId('court-result');courtResultBanner.hidden=!courtResult;document.querySelector('.court-wrap')?.classList.toggle('has-result',!!courtResult);if(courtResult){byId('court-result-title').textContent=courtResult.title;byId('court-result-detail').textContent=courtResult.detail}
 
 byId('match-panel').innerHTML=`<section class="match-settings"><h3>Make it your game.</h3><div class="eyebrow">${match.practice?'PATTERN PRACTICE · UNSCORED':'FREE PLAY · SIDE-OUT SCORING'}</div><label for="practice-pattern">Practice focus</label><select id="practice-pattern"><option value="">Free play · no lesson</option>${PATTERNS.map(p=>`<option value="${p.id}" ${match.practice===p.id?'selected':''}>${p.name}</option>`).join('')}</select><button id="start-pattern">Start selected practice / game</button>${match.practice?`<p>${PATTERNS.find(p=>p.id===match.practice)!.cue} · Variation ${match.variation+1}</p>`:''}<h2>${score.score.home} : ${score.score.away}</h2>${match.practice?'<p>Mid-rally practice · opening bounces already satisfied.</p>':`<p>Serve call <strong>${score.call}</strong> · ${score.server==='you'?'You':score.server==='partner'?'Finn':score.server==='opponent-left'?'Jules':'Rio'} serving</p>`}<details><summary>Opponent brain</summary><label for="brain-mode">Decision engine</label><select id="brain-mode"><option value="local" ${match.brainMode==='local'?'selected':''}>Local adaptive</option><option value="llm" ${match.brainMode==='llm'?'selected':''}>LLM with local fallback</option></select><label for="brain-personality">Personality</label><select id="brain-personality">${PERSONALITIES.map(p=>`<option ${match.personality===p?'selected':''}>${p}</option>`).join('')}</select><label for="brain-iq">Tactical intelligence</label><select id="brain-iq">${[.2,.5,.9].map(n=>`<option value="${n}" ${Math.abs(match.intelligence-n)<.11?'selected':''}>${n===.2?'Basic':n===.5?'Aware':'Adaptive'}</option>`).join('')}</select><button id="apply-brain">Apply for next opponent contact</button><p id="brain-status">${match.brainStatus}</p><p>Observed ${match.memory.summary().samples} recent team shots. Physical ratings are unchanged.</p></details><details><summary>Choose archetypes · starts a new game</summary>${Object.entries(PLAYER_PROFILES).map(([id,p])=>`<label for="profile-${id}">${p.name}</label><select id="profile-${id}" ${id==='you'&&match.playerDesign?'disabled':''}><option value="">${id==='you'&&match.playerDesign?'Saved Player Design skills':'Original profile'}</option>${Object.entries(ARCHETYPES).map(([key,a])=>`<option value="${key}" ${match.lineup[id as keyof typeof PLAYER_PROFILES]===key?'selected':''}>${a.name}</option>`).join('')}</select>`).join('')}<button id="apply-lineup" class="shot-button">Start game with these profiles</button></details><p class="guided-note">${match.partnerAutonomy?'Your contacts wait for you; Finn chooses his own shots.':'Every team contact waits for your choice.'}</p><details><summary>Player skills · 0–100</summary>${Object.entries(PLAYER_PROFILES).map(([id,base])=>{const custom=id==='you'?match.playerDesign:null;const p={...base,...(match.lineup[id as keyof typeof PLAYER_PROFILES]?ARCHETYPES[match.lineup[id as keyof typeof PLAYER_PROFILES]!]:{}),name:custom?escapeText(custom.name):base.name,...(custom?{skills:custom.skills,description:'Your saved Player Design skills.'}:{})};return `<h3>${p.name}</h3><p>${p.description}</p><dl class="player-skills">${Object.entries(p.skills).map(([name,value])=>`<div><dt>${name}</dt><dd>${value}</dd></div>`).join('')}</dl>`}).join('')}<p class="guided-note">Prototype attributes, not DUPR ratings. Shot skills affect execution; movement affects reach; hands affects fast volleys.</p></details></section><section class="match-play primary-choices${s.phase==='complete'?' point-result':''}"><h3>${endTitle??(playerDecision?'What’s your play?':s.currentHitter==='partner'&&match.partnerAutonomy?'Finn’s play':'Read the court.')}</h3>${playerDecision?'<p id="play-suggestion" class="play-suggestion"><strong>Suggested:</strong> A short, soft wide shot</p>':''}${s.phase==='flight'&&match.shot.feedback?`<p>Last execution · skill ${match.shot.feedback.skill} · quality ${(match.shot.feedback.quality*100).toFixed(0)}% · ${match.shot.feedback.difficulty.join(', ')} · deviation ${match.shot.feedback.deviation.toFixed(2)} m${match.shot.feedback.mishit?' · mishit':''}</p>`:''}${s.phase==='complete'?`<p class="point-result-detail">${endDetail}</p>`:match.shot.intent.type==='serve'?'':`<p>${match.shot.description}</p>`}${s.phase==='complete'?`<button id="next-point" class="shot-button">${match.practice?'Next variation':score.winner?'New game':'Next point'} ↗</button>`:playerDecision?`${primaryChoices.map(({intent,index})=>choiceButton(intent,index)).join('')}${moreChoices.length?'<button id="more-options" class="more-options-link" aria-haspopup="dialog">More options <span aria-hidden="true">→</span></button>':''}`:`<p>${match.thinking?(s.currentHitter==='partner'?'Finn is thinking…':'Opponent thinking…'):s.paused?'Paused':'Rally in progress'} · ${s.shotHistory.length} shots</p>`}</section>`;

 if(playerDecision){
 byId('match-panel').insertAdjacentHTML('beforeend',`<section class="match-play custom-composer"><label class="sr-only" for="custom-command">Call your own shot</label><p id="custom-status" role="status"></p><div id="custom-quick"></div><div class="custom-input-row"><input id="custom-command" maxlength="300" placeholder="Call your shot…" aria-label="Call your own shot"><button id="submit-command" class="custom-send" aria-label="${match.customBusy?'Interpreting custom shot':'Send custom shot'}" title="${match.customBusy?'Interpreting custom shot':'Send custom shot'}" ${match.customBusy?'disabled':''}><span aria-hidden="true">${match.customBusy?'…':'↑'}</span></button></div></section>`);
 byId('custom-status').textContent=match.customStatus;
 (byId('custom-command') as HTMLInputElement).value=match.customDraft;
 const sendCustom=()=>{void match.submitCommand((byId('custom-command') as HTMLInputElement).value).catch(e=>{match.customStatus=e.message});lastUI='';updateUI()};
 byId('custom-command').addEventListener('input',e=>match.customDraft=(e.target as HTMLInputElement).value);
 byId('custom-command').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();sendCustom()}});
 byId('submit-command').addEventListener('click',sendCustom);
 for(const [text,count] of [...match.customCounts].filter(([,n])=>n>=2).sort((a,b)=>b[1]-a[1]).slice(0,5)){const button=document.createElement('button');button.textContent=`${text} (${count})`;button.addEventListener('click',()=>{void match.submitCommand(text).catch(e=>{match.customStatus=e.message});lastUI='';updateUI()});byId('custom-quick').append(button)}
 }
 if(s.phase==='complete'){
 const firstTime=match.replayFrames[0]?.simulationTime??0,total=((match.replayFrames.at(-1)?.simulationTime??firstTime)-firstTime)/1.5;
 byId('match-panel').insertAdjacentHTML('beforeend',`<section class="match-play point-review"><h3>Point review</h3><div class="replay-player"><button id="replay-toggle" class="replay-toggle" aria-label="${match.replayPlaying?'Pause':'Play'} point replay">${match.replayPlaying?'Ⅱ':'▶'}</button><div class="replay-track"><div><strong>Replay</strong><output id="replay-time">0:00 / ${clock(total)}</output></div><input id="replay-position" type="range" min="0" max="${Math.max(0,match.replayFrames.length-1)}" value="${match.replayPosition}" step="0.01" aria-label="Replay timeline"></div>${match.replayIndex!==null?'<button id="exit-replay" class="replay-exit">Done</button>':''}</div><div class="decision-analysis"><h4>Decision analysis</h4>${analysisCards()}</div></section>`);
 byId('replay-toggle').addEventListener('click',()=>{match.replayPlaying?match.pauseReplay():match.resumeReplay();lastUI='';updateUI()});byId('replay-position').addEventListener('input',e=>{match.scrubReplay(Number((e.target as HTMLInputElement).value));lastUI='';updateUI()});byId('exit-replay')?.addEventListener('click',()=>{match.stopReplay();lastUI='';updateUI()});
 }
 byId('match-panel').insertAdjacentHTML('beforeend',`<section class="match-insights"><h3>Your session</h3><p>Patterns you’re learning to recognize.</p><details open><summary>Pattern exposure · this session</summary>${PATTERNS.map(p=>{const rows=match.records.filter(r=>r.pattern===p.id);return `<p>${p.name}: ${rows.length} decisions · ${rows.filter(r=>r.label==='Good choice').length} recognized · ${rows.filter(r=>r.label==='Risky'||r.label==='Better target available').length} review flags</p>`}).join('')}<p>Heuristic coaching, not a definitive grade. Records remain in this session only.</p></details></section>`);
 byId('start-pattern').addEventListener('click',()=>{showPanel('play');match.startPractice((byId('practice-pattern') as HTMLSelectElement).value as typeof match.practice||null);lastUI='';updateUI()});
 byId('apply-brain').addEventListener('click',()=>{match.brainMode=(byId('brain-mode') as HTMLSelectElement).value as typeof match.brainMode;match.personality=(byId('brain-personality') as HTMLSelectElement).value as typeof match.personality;match.intelligence=Number((byId('brain-iq') as HTMLSelectElement).value);lastUI='';updateUI()});
 byId('apply-lineup').addEventListener('click',()=>{for(const id of Object.keys(PLAYER_PROFILES) as (keyof typeof PLAYER_PROFILES)[]){const value=(byId(`profile-${id}`) as HTMLSelectElement).value;match.lineup[id]=value?value as keyof typeof ARCHETYPES:undefined}match.reset();showPanel('play');lastUI='';updateUI()});
 byId('next-point')?.addEventListener('click',()=>{score.winner?match.reset():match.nextPoint();lastUI='';updateUI()});
 byId('more-options')?.addEventListener('click',openShotDrawer);
 document.querySelectorAll<HTMLButtonElement>('.match-choice').forEach(button=>{
  const intent=match.availableIntents[Number(button.dataset.choice)];
  const preview=()=>{const shot=match.previewIntent(intent);if(shot){scene.setShotPreview(shot);button.classList.add('is-previewing')}};
  const clear=()=>{scene.setShotPreview(null);button.classList.remove('is-previewing')};
  let hold:number|undefined,held=false,suppressClick=false,startX=0,startY=0;
  button.addEventListener('pointerenter',event=>{if(event.pointerType==='mouse')preview()});
  button.addEventListener('pointerleave',event=>{if(event.pointerType==='mouse')clear()});
  button.addEventListener('focus',preview);button.addEventListener('blur',clear);
  button.addEventListener('pointerdown',event=>{if(event.pointerType==='mouse')return;held=false;startX=event.clientX;startY=event.clientY;hold=window.setTimeout(()=>{held=true;preview()},260)});
  button.addEventListener('pointermove',event=>{if(hold&&Math.hypot(event.clientX-startX,event.clientY-startY)>9){clearTimeout(hold);hold=undefined}});
  const release=()=>{if(hold)clearTimeout(hold);hold=undefined;if(held){suppressClick=true;held=false;clear();window.setTimeout(()=>suppressClick=false,350)}};
  button.addEventListener('pointerup',release);button.addEventListener('pointercancel',release);
  button.addEventListener('click',event=>{if(suppressClick){event.preventDefault();return}clear();if(shotDrawer.open)closeShotDrawer();match.submitIntent(intent);lastUI='';updateUI()});
 });
}
let replayAnalysisShot:number|null=null;
function syncReplayUI(replay:ReturnType<Match['replayView']>){
 const slider=document.getElementById('replay-position') as HTMLInputElement|null,time=document.getElementById('replay-time');if(!slider||!time){replayAnalysisShot=null;return}slider.value=String(match.replayPosition);const first=match.replayFrames[0]?.simulationTime??0,total=((match.replayFrames.at(-1)?.simulationTime??first)-first)/1.5,elapsed=replay?Math.max(0,(replay.state.simulationTime-first)/1.5):0;time.textContent=`${clock(elapsed)} / ${clock(total)}`;document.querySelectorAll<HTMLElement>('.analysis-shot').forEach(card=>card.classList.toggle('is-current',!!replay&&Number(card.dataset.shotIndex)===replay.state.shotIndex));const currentCard=replay?document.querySelector<HTMLElement>(`.analysis-shot[data-shot-index="${replay.state.shotIndex}"]`):null;if(match.replayPlaying&&replay&&currentCard&&replayAnalysisShot!==replay.state.shotIndex){replayAnalysisShot=replay.state.shotIndex;currentCard.scrollIntoView({behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center',inline:'nearest'})}else if(!match.replayPlaying)replayAnalysisShot=null;
}
updateUI();let previous:number|undefined;function frame(now:number){
 const realDt=previous===undefined?0:Math.max(0,Math.min((now-previous)/1000,.1)),dt=realDt*speed;if(!creator.dialog.open){if(mode==='lab')lab.update(dt);else if(mode==='match'&&match.replayPlaying)match.update(realDt);else sim.update(dt)}previous=now;updateUI();
 const replay=mode==='match'?match.replayView():null;syncReplayUI(replay);scene.setGuides(guides&&(mode!=='lab'||!lab.issue)&&!replay);scene.render(replay?.state??(mode==='lab'?lab.state:sim.state),now/1000,replay?.shot??(mode==='lab'?lab.shot:sim.shot),mode==='match'&&!match.practice&&!replay?match.scoring.call:null);requestAnimationFrame(frame)
}requestAnimationFrame(frame);
// Optional browser-native tools use the exact same validated simulation entry point.
const context=(document as Document & {modelContext?:{registerTool:(tool:unknown)=>Promise<void>|void}}).modelContext;
if(context?.registerTool){for(const tool of [{name:'read_pickleball_state',description:'Read the current rally and available shot intent.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>structuredClone({mode,match:mode==='match'?{score:match.scoring,point:match.point}:null,opponentDecision:mode==='lab'?lab.opponentDecision:null,execution:mode==='lab'?lab.execution:null,trajectory:mode==='lab'?lab.generated:null,state:mode==='lab'?lab.state:sim.snapshot(),availableIntents:mode==='lab'?(lab.issue||lab.state.phase!=='decision'?[]:(lab.decisionOptions.length?lab.decisionOptions.map(o=>o.intent):[lab.shot.intent])):sim.availableIntents})},{name:'play_pickleball_shot',description:'Submit the available shot intent during a decision pause.',inputSchema:{type:'object',properties:{intent:SHOT_INTENT_SCHEMA},required:['intent'],additionalProperties:false},execute:(input:{intent:unknown})=>{if(mode==='lab'){if(lab.decisionOptions.length)lab.chooseIntent(input.intent);if(lab.state.phase!=='decision'||!sameShotIntent(parseShotIntent(input.intent),lab.shot.intent))throw new Error('Choose the available lab shot.');lab.play()}else {if(mode==='match'&&sim.state.possession!=='home')throw new Error('Opponent is choosing.');sim.submitIntent(input.intent)};updateUI();return structuredClone(mode==='lab'?lab.state:sim.state)}}]){try{Promise.resolve(context.registerTool(tool)).catch(console.warn)}catch(error){console.warn(error)}}}
